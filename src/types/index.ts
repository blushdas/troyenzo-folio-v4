export type Social = {
  label: string;
  href: string;
  handle: string;
};

export type PortfolioItem = {
  id: string;
  title: string;
  role?: string;
  description: string;
  link?: string;
  accent: string;
};
